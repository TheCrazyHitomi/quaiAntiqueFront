import ErrorTitle from "../components/Title/errorTitle";

const ErrorPage = () => {
    return (
        <div className="text-center">
            <ErrorTitle />
        </div>
    );
}

export default ErrorPage;
